#include <stdio.h>

int main() {
    int a;

    for (a = -4; a < 4; a++)
    {
        printf ("a is equal to %d\n", a);
    }
    printf ("finished the loop and a is equal to %d\n", a);

    return (0);
}